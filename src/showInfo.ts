import { version } from '../package.json'

console.log('[GhChart] The service is running normally!')
console.log('Frontend version: v' + version + 
            '\nBackend Core version: v4.0.0' + 
            '\nBackend API version: v14')
